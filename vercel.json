{
  "buildCommand": "npm run build",
  "outputDirectory": "MusicRecommenderFront/dist",
  "installCommand": "npm install && npm --prefix MusicRecommenderFront install",
  "framework": null,
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://bin-damages-vegetarian-delivered.trycloudflare.com/api/:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
